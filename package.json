{
  "name": "minima.zig",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "build:wasm": "./build.sh",
    "build": "tsc -p tsconfig.json",
    "build:all": "pnpm run build:wasm && pnpm run build",
    "build:watch": "tsc -p tsconfig.json --watch",
    "clean": "rm -f dist/index.mjs dist/index.wasm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
