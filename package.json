{
  "name": "minima.zig",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:wasm": "./build.sh",
    "build:all": "pnpm run build:wasm && pnpm run build",
    "fmt": "zig fmt src && tsc -p tsconfig.json --noEmit",
    "dev:ts": "tsc -p tsconfig.json --watch",
    "dev:server": "go run main.go",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}